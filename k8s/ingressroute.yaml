apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: ingressroute
spec:
  entryPoints:
    - web
  routes:
  - match: Host(`calc.ucommerce.com.br`)
    kind: Rule
    services:
    - kind: Service
      name: calculadora-service
      port: 80